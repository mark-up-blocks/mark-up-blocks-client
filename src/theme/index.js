const color = {
  main: "#FBC00E",
  inner: "#fff",
  focus: "#ffddad",
  text: "#000000",
  inactive: "#808080",
  error: "#d22020",
};

const border = {
  container: "2px solid #FBC00E",
  radius: {
    container: "4px",
  },
};

const screenSize = {
  maxWidth: {
    mobile: "600px",
  },
};

const theme = {
  color,
  border,
  screenSize,
};

export default theme;
